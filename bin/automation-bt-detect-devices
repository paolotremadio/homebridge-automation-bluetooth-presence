#!/usr/bin/env node

'use strict';

process.title = 'automation-bluetooth-presence-detect-devices';

var path = require('path');
var fs = require('fs');
var lib = path.join(path.dirname(fs.realpathSync(__filename)), '..');

require(lib + '/detect')();
